package game;

import java.net.ServerSocket;
import java.net.Socket;

public class Client {
    private static Socket serverSocket;
    private static Socket clientSocket;
    
    private static connectToServer( String host, int port ) {
        
    }
}
